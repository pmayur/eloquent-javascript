/* function multiplier(factor) {
    this.feku = function(number){
        number * factor;
    }
    return feku;
}
let twice = multiplier(2);

console.log(twice(5)); */



function multiplier(factor) {
    return number => number * factor;
    }
    let twice = multiplier(2);
    console.log(twice(5));


